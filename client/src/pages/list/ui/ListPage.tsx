import { Typography } from '@mui/material'

export const ListPage = () => {
    // позже подключим widgets/AdList, features/ad-filters и т.д.
    return <Typography variant='h4'>Список объявлений</Typography>
}
